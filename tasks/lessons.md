# Lessons

- When a required runtime is missing in WSL, install it in the user environment if possible instead of treating missing `node`/`npm`/`pnpm` as an acceptable verification limitation. Prefer home-directory tooling such as `nvm` when `sudo` needs a password.
